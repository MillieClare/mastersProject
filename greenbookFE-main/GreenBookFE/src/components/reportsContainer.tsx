import { Grid } from "@mui/material";

const ReportsContainer = () => {
  return (
    <Grid container spacing={4}>
      console.log("here goes the report card")
    </Grid>
  );
};

export default ReportsContainer;
